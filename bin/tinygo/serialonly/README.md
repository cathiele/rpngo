This is a simple "starting place" for using a microcontroller with [TinyGo](https://tinygo.org/).

Example:

```
tinygo flash -target=pico -monitor
```

Set `-target` to your microcontroller, noting that the code likely will not fit
on lower-spec models.

